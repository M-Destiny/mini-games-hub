services:
  - type: web
    name: mini-games-server
    repo: https://github.com/M-Destiny/mini-games-hub.git
    env: node
    region: oregon
    buildCommand: cd server && npm install
    startCommand: cd server && node index.js
    rootDirectory: server
    envVars:
      - key: NODE_ENV
        value: production
